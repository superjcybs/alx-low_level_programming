#include <stdio.h>
/**
 * main - entry point
 * Desc: will print the name of this program
 * @argc: no. of arguments
 * @argv: array of pointers to the arguments
 * Return: returns 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
