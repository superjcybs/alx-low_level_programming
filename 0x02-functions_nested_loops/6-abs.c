#include "main.h"
/**
 * _abs - entry points
 *
 * Description: computes the value of an integer
 * @n: the integer to work on
 *
 * Return: the value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
